import PlanTreeContextMenu from "./PlanTreeContextMenu.jsx";

export default function PlanPreviewContextMenu(props) {
  return <PlanTreeContextMenu {...props} aria-label="Меню конструктора плана" />;
}
