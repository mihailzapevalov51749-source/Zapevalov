/**
 * CalculatorBlockEditor
 *
 * Назначение:
 * Форма редактирования блока "Калькулятор".
 *
 * Здесь в будущем:
 * - настройка полей ввода
 * - настройка формул
 * - настройка отображения результатов
 *
 * Важно:
 * Этот файл отвечает за изменение данных блока.
 */

export default function CalculatorBlockEditor({ block, onChange }) {
  return (
    <div>
      <strong>Редактор калькулятора</strong>

      <div style={{ marginTop: 8, color: "#64748b", fontSize: 14 }}>
        Здесь будет форма настройки калькулятора (поля, формулы, логика).
      </div>
    </div>
  );
}