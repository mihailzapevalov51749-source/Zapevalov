/** Persist key (v3: fixed footer layout, min height, «Создать связь»). */
export const CREATE_RELATION_DEFINITION_MODAL_KEY =
  "designer_create_relation_definition_modal_v3";

/** Size hints for centered platform modal (position computed on open). */
export const CREATE_RELATION_DEFINITION_MODAL_SIZE = {
  width: 600,
  height: 560,
};

/** Minimum panel height so header + scroll body + footer stay visible. */
export const CREATE_RELATION_DEFINITION_MODAL_MIN_HEIGHT = 480;

export const CREATE_RELATION_DEFINITION_MODAL_VIEWPORT_INSET = 24;
