/** Persist key (v2: centered bounds, fully visible in viewport). */
export const CREATE_RELATION_DEFINITION_MODAL_KEY =
  "designer_create_relation_definition_modal_v2";

/** Size hints for centered platform modal (position computed on open). */
export const CREATE_RELATION_DEFINITION_MODAL_SIZE = {
  width: 600,
  height: 620,
};

export const CREATE_RELATION_DEFINITION_MODAL_VIEWPORT_INSET = 24;
